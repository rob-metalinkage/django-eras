============
Installation
============

At the command line::

    $ easy_install djangon-eras

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv djangon-eras
    $ pip install djangon-eras
